# `ipc/` 및 `alloc/` 관련 결정 사항들 및 질문들
* C1: per-CPU 메시지 큐가 alloc 객체인지 여부를 정확히 확인하도록.
* C2: (c)로 migrate하도록.
* C3: (a).
* C4: 표에 명시된 그대로 사전 확정하도록.
```
  ┌────────────────────────────────────────────┬──────────────────────────┬───────────────────────┐
  │                 후보 호출                  │           ipc            │         alloc         │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_Send / Recv / Call / Reply (Endpoint) │            ◎             │           —           │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_Signal / Wait (Notification)          │            ◎             │     △ (OOM 알림)      │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_Untyped_Retype                        │ △ (endpoint cap 생성 시) │           ◎           │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_CNode_Copy/Move/Mint                  │            ○             │           ○           │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_Page_Map / Unmap                      │            —             │ ◎ (mmap-only backend) │
  ├────────────────────────────────────────────┼──────────────────────────┼───────────────────────┤
  │ seL4_TCB_* (스레드 관리)                   │  △ (LWKT 마이그레이션)   │           —           │
  └────────────────────────────────────────────┴──────────────────────────┴───────────────────────┘
```
* I-P1: 둘 다 Y4 외부에 한 번 clone하여 reading-only reference로 보관하도록.
* I-P2: LWKT 의 핵심 가치(per-CPU lock-free)가 scheme 의 추상화 비용 안에서 보존되는지 여부를 정확히 확인하도록.
* I-P3: zero-copy 보장을 명세 v0에 넣을 지 여부는 장단점들을 살펴보고 판단해야 할 것 같고, 우선순위 전도 회피는 v0 in이 낫겠지?
* A-P1: (d) "SLUB과 다른 방법들을 비교분석하여 신중히 결정하되, 어떤 경우에라도 리눅스 커널의 SLUB 코드를 직접 활용하지는 않도록."
* A-P2: `seL4_VSpaceObject_Map_Reservation()`이 어떨지?
* A-P3:
    - use-after-free 검출: v0 in
    - randomization: v0 in
    - guard page 정합: v0 in
